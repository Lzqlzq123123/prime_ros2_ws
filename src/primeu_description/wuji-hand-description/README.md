This directory is present because `primeu_description` references assets under `wuji-hand-description/`.

In the source workspace used for this upload, the directory itself existed but did not contain the referenced files.

If you need the hand meshes, populate this directory from the expected upstream source before using the hand-enabled URDF/MJCF assets.
