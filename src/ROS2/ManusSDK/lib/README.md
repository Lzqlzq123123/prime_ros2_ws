The original local workspace contained two MANUS SDK shared libraries here:

- `libManusSDK.so`
- `libManusSDK_Integrated.so`

They were not included in this GitHub upload because each file exceeds GitHub's 100 MB per-file limit.

Restore them locally before building `manus_ros2` if your setup depends on these binaries.
